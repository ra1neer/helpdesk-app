name: .NET Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0
    
    - name: Restore dependencies
      run: dotnet restore Helpdesk/Helpdesk.csproj
    
    - name: Build
      run: dotnet build Helpdesk/Helpdesk.csproj --no-restore
    
    - name: Run tests
      run: dotnet test Helpdesk/Helpdesk.csproj --no-build --verbosity normal
